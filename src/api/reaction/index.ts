import runDeclineReaction from './decline';
import runErrorReaction from './error';
import runSuccessReaction from './success';

export const actionFns = {
    runDeclineReaction,
    runErrorReaction,
    runSuccessReaction,
};

export default actionFns;
