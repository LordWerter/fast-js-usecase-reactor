import breakPipeline from './break';
import restartPipeline from './restart';
import runPipeline from './run';

export const actionFns = {
    breakPipeline,
    restartPipeline,
    runPipeline,
};

export default actionFns;
